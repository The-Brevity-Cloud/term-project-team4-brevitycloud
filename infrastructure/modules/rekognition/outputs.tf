# No outputs needed from this module anymore, as resources are created elsewhere
# or the necessary values (like API GW ID) are passed in. 

# The invoke lambda outputs previously added here were incorrect,
# as the lambda is created in the lambda module. 